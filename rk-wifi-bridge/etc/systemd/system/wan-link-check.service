[Unit]
Description=WAN connectivity probe (toggles eth0 link based on wlan0 upstream reachability)
After=NetworkManager.service
Wants=NetworkManager.service

[Service]
Type=simple
ExecStart=/usr/local/sbin/wan-link-check
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
